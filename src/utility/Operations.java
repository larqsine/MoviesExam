package utility;

public enum Operations {
    GET_NEXT(), GET_PREVIOUS(), GET_CURRENT_MOVIE(),
    PLAY_NEXT(), PLAY_PREVIOUS(), PLAY(), PAUSE(), PLAY_CURRENT(),
    GET_DEFAULT();

    Operations() {
    }
}
